generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model InvoiceRequest {
  id                 String   @id @default(cuid())
  takerCnpj          String
  serviceCity        String
  serviceState       String
  serviceValue       Float
  desiredIssueDate   DateTime
  serviceDescription String
  status             StatusNotaFiscal @default(PENDENTE_EMISSAO)
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  // Campos para quando a nota é emitida
  invoiceNumber      String?
  invoiceIssueDate   DateTime?

  @@map("solicitacoes_notas_fiscais")
}

enum StatusNotaFiscal {
  PENDENTE_EMISSAO
  EMITIDA
  CANCELADA
}

